# ============================================================================
# easy-for-gaokun — .gitignore
# ============================================================================

# ---------------------------------------------------------------------------
# ★ 红线一：内部台账，严禁公开
#   AGENTS.md 只是本地 agent 的记忆文件，不属项目交付物。
# ---------------------------------------------------------------------------
AGENTS.md

# ---------------------------------------------------------------------------
# ★ 红线二：专有资产，绝不入库
#   1) Drv/       Windows 备份驱动转储（从 Windows 侧拷出，~1153 MB）
#   2) Recovery/  原厂恢复镜像转储（~3987 MB）
#    体积不可接受，且公开分发存在版权与许可风险。
# ---------------------------------------------------------------------------
Drv/
Recovery/

# ---------------------------------------------------------------------------
# 构建产物
# ---------------------------------------------------------------------------
*.o
*.ko
*.mod
*.mod.c
*.cmd
*.symvers
*.order
*.a
*.so
*.so.*
*.elf
*.deb
*.rpm
*.AppImage
*.img
*.iso
*.wic
*.raw

# 内核/DTB 构建中间产物（补丁源码 *.dts / *.dtsi / *.patch 仍入库）
*.dtb
*.dtbo
*.dtb.tmp

# 补丁冲突残留
*.orig
*.rej
*.patch.rej

# ---------------------------------------------------------------------------
# 下载与构建缓存
# ---------------------------------------------------------------------------
.cache/
cache/
downloads/
dist/
out/
build/
builddir/
_root/

# ---------------------------------------------------------------------------
# 本地环境、凭据与日志 —— 绝不入库
# ---------------------------------------------------------------------------
.env
.env.*
*.local
secrets/
*.pem
*.key
*.p12
*.jks
*.log
ssh-askpass*

# ---------------------------------------------------------------------------
# 编辑器 / 操作系统
# ---------------------------------------------------------------------------
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
desktop.ini
