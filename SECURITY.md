# 安全策略

本仓库包含**修改设备引导配置与内置存储**的脚本、面向 Linux 内核的设备树与驱动补丁，
以及一台**没有救援通道**的 ARM 平板上的实机适配成果。因此这里的安全问题不止于软件漏洞，
还包括**可能让设备彻底无法启动的操作风险**。

---

## 1. 支持的版本

本项目**只针对一个机型、一个基线镜像**提供支持：

| 项目 | 受支持的值 |
| ------ | ------------ |
| 机型 | HUAWEI MateBook E Go **2022 性能版**（GK-W76 / SC8280XP / 设备树 `gaokun3`） |
| 基线镜像 | `KawaiiHachimi/linux-gaokun-buildbot` release `ubuntu26.04-7.1.0-rc3-gaokun3+el2-20260514004329` |
| 仓库版本 | `main` 分支，以及 README 中列出的**已发布**内核产物 |

**不受支持**（相关问题不予受理，也不视为本项目安全范围）：

- 同系列的其他变体：**2022 LTE 版**（8cx Gen 2 / SC8180X / `gaokun2`）与
  **2023 版**（8cx Gen 3 降频版）。
- **官方 Ubuntu ISO 环境**。官方 ISO 在本机型上无法正常启动，原因见 [README §3](README.md#3-基线系统)。
- 自行替换的内核、自行修改的设备树、第三方构建的镜像。
- 已发布的 Release 产物在**非目标机型**上的行为。

机型鉴别判据见 [CONTRIBUTING.md 的「目标设备」](CONTRIBUTING.md#目标设备)；
基线之外的环境请先在 issue 中说明，再讨论是否属于本项目问题。

---

## 2. 威胁模型

### 2.1 本机型没有 EDL / 9008 救援通道

**这是本项目最重要的安全前提。** GK-W76 **不存在** Qualcomm EDL（Emergency Download，9008）
或其他低层刷写通道。这意味着：

| 被破坏的对象 | 后果 |
| -------------- | ------ |
| 分区表（GPT） | **无法救回**，内置盘上的 Windows 与 Ubuntu 同时不可用 |
| ESP（EFI 系统分区） | **无法救回**，引导链中断，且没有可依赖的恢复入口 |
| UEFI 引导项 / 引导顺序 | 设备可能直接进入无法选择的启动状态 |
| 内置存储上的任意数据 | 一旦覆盖即永久丢失（无原厂恢复分区以外的退路） |

因此，**任何一条"改错了就重刷"的常规经验在本机型上都不成立**。项目对涉及上述对象的操作，
一律按不可逆操作对待。

### 2.2 需要设备所有者显式确认的高危操作

以下操作**在没有设备所有者明确确认的情况下禁止执行**，脚本也不得默认启用：

- **修改分区表**（`sgdisk` / `parted` / `fdisk` 写入，以及任何等价的 GPT 改写）。
- **覆盖 ESP**（向 EFI 系统分区写入或删除文件，包括 `dd` 与 `rsync --delete`）。
- **改写引导项**（`efibootmgr` 的新建、重排、删除，以及 `loader.conf` 默认项的改动）。
- **`dd` 到内置存储**（任何以 `nvme0n1` 或其分区为输出目标的 `dd`）。

**涉及上述操作的脚本必须默认 `DRY_RUN`**：默认只打印将要执行的动作，
必须由操作者显式关闭空跑或逐项确认后才会真正写入。
这条要求与 [CONTRIBUTING.md 的「安全约束」](CONTRIBUTING.md#安全约束) 一致，
也是审查此类脚本改动的第一判据。

### 2.3 其他风险类别

| 类别 | 说明 |
| ------ | ------ |
| 脚本提权与误伤 | 诊断类脚本应尽量**只读**；改动类脚本必须提供精确撤销路径（例如 `--revert` / `--uninstall`），并在文档中写明回滚方式 |
| 引导链完整性 | 引导相关改动必须**只新增条目、不改动既有默认条目与 EFI 变量**，以便随时回退到已验证的启动项 |
| 固件与驱动许可 | 厂商专有驱动与固件**不得入库、不得作为附件上传**（见下节），由此产生的版权与再分发风险由提交者承担 |
| 供应链 | Release 产物应附 SHA-256 校验与构建溯源清单；不要使用来源不明的内核、镜像或固件 |

### 2.4 不要提交厂商专有驱动或固件

**`Drv/` 与 `Recovery/` 目录永不入库。** 它们分别是 Windows 全量驱动转储与原厂恢复镜像转储，
属于华为 / 高通的专有软件，且体积不可接受（合计约 5 GB）。
两者已在 `.gitignore` 中排除；请**不要**以 issue 附件、Release 资产或 PR 的形式绕过这一限制提交。

需要引用其中的观察结论时，只记录**可复现的取证方法与结论**，
不要把原始二进制、固件或反编译产物带进仓库。

---

## 3. 报告漏洞

### 3.1 优先使用 GitHub 私密渠道

**请优先通过 GitHub Security Advisories 的 Private vulnerability reporting 上报**
（仓库的 **Security** 标签页 → **Report a vulnerability**）。
该渠道对本项目尤其重要，因为报告中往往需要包含：

- 可能破坏引导或分区的操作序列；
- 设备序列号、分区 UUID 等**可用于识别设备的信息**；
- 尚未公开的驱动、固件或设备树缺陷。

**请不要先开公开 issue。** 公开披露会让仍在受影响状态下的用户暴露在风险中，
也会使修复方案在合入前就被动公开。维护者会在修复可用、或在双方约定的时间点再公开说明。

若私密上报入口不可用，请通过维护者的 GitHub 账号联系（名单见 [`MAINTAINERS.md`](MAINTAINERS.md)），
**不要**在公开渠道粘贴上述敏感细节。

### 3.2 报告中请包含

1. **机型三判据的实际输出**（设备树 `compatible`、大核最高频率、面板与触屏枚举），
   或 `scripts/00-preflight.sh` 的完整输出。
2. **内核版本**（`uname -a`）。
3. **`scripts/90-report.sh` 的完整报告**（若其中含设备标识信息，请在私密渠道提交）。
4. **复现步骤**：做了什么、预期什么、实际发生了什么。
5. **影响评估**：是否涉及数据丢失、引导不可用，或仅影响功能。

缺少上述信息会显著拖慢定位。完整的提问要求见 [`SUPPORT.md`](SUPPORT.md)。

### 3.3 响应预期

本项目由社区维护者利用业余时间维护，**不提供 SLA 或响应时间承诺**。
可参考的期望是：**尽力在 7 天内**给出初步回应（确认收到、请求补充信息，或给出初步判断）。
复杂问题、需要实机复现的问题，以及需要与上游内核社区协作的问题，耗时会长于该区间。

维护者的处理顺序：

1. 确认报告并评估是否触及 §2.2 的高危操作面；
2. 在目标设备上复现（**无法在目标设备上复现的改动不合入**，见 [`GOVERNANCE.md`](GOVERNANCE.md)）；
3. 修复并给出回滚方式；
4. 在私密渠道与报告者确认后，再决定公开披露的时机与形式。

### 3.4 不属于安全报告的事项

- 功能缺失、性能不佳、音质或显示效果不满意 → 走 issue（见 [`SUPPORT.md`](SUPPORT.md)）。
- 非目标机型或官方 Ubuntu ISO 环境的失败 → 不属于本项目支持范围。
- 硬件改装的可行性咨询 → 不在本项目范围内。

---

## 4. 安全相关的仓库约定

- 所有文件以 **UTF-8（无 BOM）+ LF** 行尾保存，脚本必须通过 `bash -n`。
- 改动类脚本必须支持 `DRY_RUN`，并在 `DRY_RUN` 下只打印将要执行的动作。
- 涉及系统状态的改动必须写明**回滚方式**。
- 内核补丁与设备树派生自 Linux 内核（GPL-2.0），保留原有版权信息与 `Signed-off-by` 链。
- 本仓库以 **GPL-2.0-only** 发布，详见 [`LICENSE`](LICENSE)。
