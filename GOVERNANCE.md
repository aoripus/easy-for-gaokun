# 项目治理

本文件说明 **easy-for-gaokun** 的角色划分、决策方式、上游化原则与发布权归属。
目标是让项目的每一个结论都可追溯到**一台真实设备上的一次真实验证**，
并让本地修复尽可能回流到上游 Linux。

---

## 1. 项目定位

**easy-for-gaokun** 是面向 **HUAWEI MateBook E Go 2022 性能版（GK-W76 / SC8280XP / `gaokun3`）**
的 Linux（Ubuntu）专项适配工程。它不是研究笔记仓库，而是可复现、可验证、可上游化的工程交付物：

```text
诊断工具 → 修复脚本 → 补丁 → 文档 → 一键化安装
```

受支持的机型与基线镜像见 [README](README.md) §1 与 §3，以及 [`SECURITY.md`](SECURITY.md) §1。

---

## 2. 角色

| 角色 | 含义 | 权限 |
| ------ | ------ | ------ |
| **Reporter（报告者）** | 在目标设备上使用本项目、并按 [`SUPPORT.md`](SUPPORT.md) 提供实机证据的人 | 开 issue、参与讨论、提交 PR |
| **Contributor（贡献者）** | 其改动被合入过本仓库的人：补丁、脚本、文档、实测记录 | 在 Reporter 基础上，参与方案讨论与 review 意见 |
| **Maintainer（维护者）** | 对仓库有写权限、对改动有最终裁定权、负责发布与安全响应的人 | 合入改动、裁定分歧、打 tag 与发 release、处理私密安全报告 |

**所有角色都必须持有目标设备。** 这是本项目的硬性前提：没有实机，就无法参与
"能复现 / 已验证"的判断，也就无法承担对应角色的责任。名单见 [`MAINTAINERS.md`](MAINTAINERS.md)。

角色不是头衔而是责任：Reporter 的责任是提供可信证据，Contributor 的责任是让改动可复现，
Maintainer 的责任是守住基线、守住设备安全、并让修复回流上游。

---

## 3. 决策方式

### 3.1 维护者最终裁定

本项目的分歧解决方式是**维护者最终裁定（maintainer-decides）**，
而不是投票或共识门槛。理由很直接：项目只支持一台设备，
判断"这个改动在这个机型上是否成立"需要实机，而能稳定提供实机验证的人极少。
投票会把决策权交给无法验证结论的人。

### 3.2 实机证据优先

裁定依据的优先级：

1. **目标设备上的实测输出**（`dmesg`、`/proc/interrupts`、脚本原始输出、字节级比对）；
2. **一手文件与代码**（内核源码、驱动源码、设备树、固件与注册表取证）；
3. **厂商侧行为**（Windows 驱动、固件实现）——作为"硬件真实行为"的旁证；
4. **社区报告与论坛结论**——仅作线索，**不能单独作为裁定依据**。

文档中的结论必须标注来源与置信度：**【已核实】/【社区报告】/【推测】**。
未标注来源的结论在 review 中会被要求补齐。

**反例留存**：被实测推翻的假设不会被删除，而是记录在文档中并标明"已被推翻及原因"。
本项目已经出现过两轮这样的事故（把"写 sysfs"的副作用误判为 AFE 命令语义、
只看空闲数据就推断负载下的中断行为），保留这些记录是为了避免重复踩坑。

### 3.3 硬规则：无法在目标设备上复现的改动不合入

**这是不可协商的硬规则。** 任何功能改动在合入前必须满足：

- 在**目标设备**（2022 性能版 + 受支持基线）上执行过；
- 提供了可复现的命令与**原始输出**（只写"已实测通过"不算证据）；
- 若涉及系统状态，写明了**回滚方式**。

不满足上述条件的改动**不合入**，无论其看起来多么合理。
由维护者裁定为"仅影响文档、不改变行为"的改动不受此限。

### 3.4 涉及设备安全的高危改动

触及 [CONTRIBUTING.md「安全约束」](CONTRIBUTING.md#安全约束) 所列举高危操作面
（修改分区表、覆盖 ESP、改写引导项、`dd` 到内置存储）的改动，
除上述要求外还必须满足：脚本**默认 `DRY_RUN`**、需要设备所有者显式确认、
并提供精确的撤销路径。本机型**没有 EDL / 9008 救援通道**，详见 [`SECURITY.md`](SECURITY.md) §2。

---

## 4. 上游优先原则

**本地修复优先做成可上游补丁。**

本仓库的目标不是维护一个永久分叉，而是把 gaokun3 的适配工作推回上游 Linux。
因此：

| 情形 | 处置 |
| ------ | ------ |
| 修复上游能接受 | 拆成**最小、单一主题**的补丁，按内核社区规范（`git format-patch`、`Signed-off-by`、正文写清现象与根因、附实机验证方式）提交上游 |
| 修复暂不可上游（依赖未入树部件、需要厂商信息） | 保留在 `patches/` 下，并在补丁说明中写清**为何现在不能上游**、以及**什么条件下可以重启上游化** |
| 上游已有等价实现 | 直接采用上游版本，删除本地补丁 |

放置与命名规范：内核补丁统一放在 `patches/`，命名与内核补丁一致；
每个补丁序列应说明**上游状态**与**基线版本**，避免出现"补丁已在成品中被吸收、却被重复施加"的情况。

**可上游性本身是评审标准之一**：一个只能以仓库内分叉形式存在、且无法说明原因的改动，
需要维护者明确同意才会合入。反之，一个已被上游接受、或明显具备上游价值的修复，优先级最高。

---

## 5. 重大决策与 ADR

影响面超出单个文件的决策，必须写成 **ADR（Architecture Decision Record）**，
存放在 `docs/adr/` 下，与本仓库文档并列管理。ADR 目录与索引由文档治理工作维护。

**必须走 ADR 的决策类型：**

- **运行级别选择**（`el1` / `el2`）：视频硬解与 KVM 的取舍；
- **触屏通路选择**（SPI vs I²C-HID）等涉及设备树结构性改动的路线裁决；
- **空闲与功耗策略**：默认值变更，例如触屏空闲策略的触发阈值；
- **产物命名与发布规范**：内核串字段、修订号 `r<n>` 的语义与裁决规则；
- **基线升级**：更换受支持的基线镜像或内核源码树；
- **许可与依赖**：引入新的上游资产或改变许可兼容性的做法。

**ADR 的格式要求：**

| 小节 | 内容 |
| ------ | ------ |
| 标题与编号 | `NNNN-<短横线小写主题>` |
| 状态 | `提议中` / `已接受` / `已被取代` / `已废弃` |
| 背景 | 现象、约束与已核实的事实（标出来源与置信度） |
| 决策 | 选定了什么，以及**否决了哪些备选方案** |
| 后果 | 收益、代价、回滚方式 |
| 证据 | 指向实测记录、`docs/bench/` 原始数据或补丁的路径 |

**ADR 不回溯修改**：决策被推翻时，新增一份 ADR 并在旧 ADR 中标注"已被 `NNNN` 取代"，
而不是改写历史结论。这与本仓库"保留被推翻的假设"的做法一致。

---

## 6. 发布权

- **只有 Maintainer 可以打 tag 与发布 Release。**
- 源码里程碑使用 `vX.Y.Z` 标签；**二进制内核构建使用独立标签**，
  命名形如 `kernel-<完整内核串>-<YYYYMMDD>`。
- 内核串、修订号 `r<n>` 的裁决规则，以及"标题/说明可随时修改但不产生新版本"的约定，
  见 [README「构建产物与发布」](README.md#7-构建产物与发布)。
- Release 中的内核产物必须附带**构建溯源清单**与 **SHA-256 校验**，
  以满足 GPL-2.0 的对应源码要求并保证可复现。
- **未做启动验证的产物**必须在 Release 说明中显式标注为实验性。
- 已被外部引用的 tag **不重命名、不移动**；确需修订时按命名规范递增修订号并新建 tag。

流程细节与发布检查单见 `docs/releasing.md`。

---

## 7. 分支与提交

- 默认分支为 `main`。Contributor 从 `main` 切出分支，命名体现改动性质：
  `feat/…`、`fix/…`、`docs/…`。
- **一个逻辑改动一个提交**；提交信息用 Conventional Commits，
  格式 `<type>(<scope>): <subject>`，首行不超过 72 字符。
- 脚本改动与文档改动不要混在同一次提交里。
- 完整规范见 [`CONTRIBUTING.md`](CONTRIBUTING.md)。

---

## 8. 安全与行为准则

- **安全漏洞与引导风险**：走私密渠道上报，不要先开公开 issue，见 [`SECURITY.md`](SECURITY.md)。
- **行为准则**：见 [`CODE_OF_CONDUCT.md`](CODE_OF_CONDUCT.md)，
  举报与安全上报共用 GitHub 私密渠道。
- **问答与支持范围**：见 [`SUPPORT.md`](SUPPORT.md)。

---

## 9. 修订本文件

GOVERNANCE 的改动属于**治理决策**：由 Maintainer 提出并合入，
改动理由需在提交正文中说明；若改动影响决策方式、角色定义或发布权，
应同时新增一份 ADR 到 `docs/adr/`。

本文件以 **GPL-2.0-only** 发布，与仓库其余部分一致，详见 [`LICENSE`](LICENSE)。
