# 贡献指南

感谢你愿意为 **easy-for-gaokun** 投入时间。

本项目是一个 Linux 适配工程：让 **Ubuntu** 在 HUAWEI MateBook E Go 2022 性能版上获得完整、开箱可用的体验。仓库里的脚本、补丁与文档都是**实机结论**的沉淀，不是研究笔记——每一处改动都应当能被复现、被验证，并尽可能向上游提交。

## 前置条件：你必须持有目标设备

**本项目的全部结论只在 §目标设备 所描述的那一台机器上成立。** 设备树的引脚配置、面板参数、触屏接口模式、双系统安装步骤，都来自该机型的实机验证；换成同名系列的其他变体（LTE 版、2023 版），结论可能完全不同。

- 没有实机就不要提交功能改动。无法在真机上复现的改动不会被合并。
- 提 issue 前请先按 §报告问题前 完成自检。

## 目标设备

| 项目 | 值 |
|------|-----|
| 商品名 | HUAWEI MateBook E Go 2022 性能版 |
| 市场型号 | GK-W76 |
| SoC | Snapdragon 8cx Gen 3（SC8280XP） |
| 设备树代号 | `gaokun3` |
| 设备树 compatible | `huawei,gaokun3` + `qcom,sc8280xp` |
| 大核最高频率 | `2995200` kHz |
| 内屏 | 12.35" 2560×1600，CSOT `ppc357db1-4` |
| 触摸 | Himax HX83121A，SPI 接口（`spi0.0`） |

三条可靠判据必须同时满足：

```bash
# 1. 设备树 compatible：应包含 huawei,gaokun3 与 qcom,sc8280xp
tr '\0' '\n' < /proc/device-tree/compatible

# 2. 大核最高频率：应为 2995200
cat /sys/devices/system/cpu/cpu7/cpufreq/cpuinfo_max_freq
```

第三条（面板与触屏是否按预期枚举）由 `scripts/00-preflight.sh` 自动校验，请直接运行它。

**`GK-W76` 不能单独作为判据。** 它只是市场型号字符串：目标机的 DMI 产品名读作 `GK-W7X`，设备树 `model` 只写 `Matebook E Go`。只有设备树 `compatible` 与大核最高频率可以区分变体。

| 变体 | SoC / 设备树 | 支持状态 |
|------|--------------|----------|
| 2022 性能版 | Snapdragon 8cx Gen 3 / SC8280XP / `gaokun3` | ✅ 唯一支持 |
| 2022 LTE 版 | Snapdragon 8cx Gen 2 / SC8180X / `gaokun2` | ❌ 不支持 |
| 2023 版 | 8cx Gen 3 降频版（约 2.69 GHz） | ❌ 不支持 |

## 可以贡献什么

### 设备树与内核补丁（上游化）

修复或补充 `gaokun3` 的设备树与驱动：引脚复用、面板、触屏、EC 等。目标是让本地修复变成可以提交上游 Linux 的补丁，而不是永久停留在仓库内的分叉。补丁统一放在 `patches/`，命名与内核补丁一致，例如 `patches/0001-arm64-dts-qcom-sc8280xp-huawei-gaokun3-select-SPI-mode-for-touchscreen.patch`。

### 适配脚本（`scripts/`）

新增或改进诊断与修复脚本，放在 `scripts/` 下；编号体现执行阶段（`00-` 前检、`20-` 触屏修复……）。公共函数一律复用 `scripts/lib/common.sh`。

### 文档与实测记录

在 `docs/` 下补充可复现的步骤、判据与踩坑记录，例如 `docs/install-dualboot.md`。文档中的每个结论都应写明是实机实测还是社区报告。

### 实测反馈（issue）

附上 `90-report.sh` 的完整输出，以及 §报告问题前 要求的信息。没有实机数据的现象描述无法定位。

### 当前最需要帮助的方向

| 方向 | 现状 | 需要的帮助 |
|------|------|-----------|
| 视频硬解 | `qcom-venus` 拒绝 SC8280XP | 定位拒绝原因，给出可用的硬解方案或补丁 |
| 音频音质 | 四扬声器未达预期 | 排查功放通路与调音，给出可复现的改进 |
| 手写笔通道 | Linux 侧从未实现 | 在实机上从零实现笔输入通路，并给出实机验证 |

## 报告问题前

请提供以下信息，缺任意一项都无法开始定位：

1. **三条机型判据的实际输出**：设备树 `compatible`、大核最高频率、面板与触屏的枚举结果；或直接给出 `scripts/00-preflight.sh` 的完整输出。
2. **内核版本**：`uname -a`。
3. **`90-report.sh` 的完整报告**。
4. **复现步骤**：做了什么、预期是什么、实际是什么。

关于基线：本项目唯一受支持的镜像是社区构建 `KawaiiHachimi/linux-gaokun-buildbot` 的 release `ubuntu26.04-7.1.0-rc3-gaokun3+el2-20260514004329`。**官方 Ubuntu ISO 无法在这台机器上启动**，基于官方 ISO 的报告不予处理。

> 三个变体不要混淆。拿 2022 LTE 版或 2023 版的输出报 2022 性能版的问题，只会浪费双方的时间。

## 提交代码的规范

### 分支与提交粒度

- 从 `main` 切出分支，命名体现改动性质：`feat/…`、`fix/…`、`docs/…`。
- **一个逻辑改动一个提交**。脚本改动与文档改动不要混在同一次提交里；格式调整不要夹带在功能改动里。

### 提交信息格式

格式为 `<type>(<scope>): <subject>`：

- `type` 取值：`feat`、`fix`、`docs`、`chore`、`refactor`、`test`、`perf`、`build`。
- 首行不超过 **72 字符**，使用**简体中文**，不以句号结尾。
- 正文说明「改了什么、为什么」，必要时引用实机输出。

```text
feat(scripts): 新增触屏诊断与修复脚本（gpio174 接口模式）
fix(dts): 为触屏默认引脚状态补充 gpio174 低电平输出
docs(install): 补充双系统安装中 ESP 空间不足的处置记录
test(scripts): 为前检脚本补充大核最高频率判据断言
```

### Shell 脚本要求

- Bash 5 编写，开头使用 `set -euo pipefail`。
- `bash -n` 必须通过。
- 日志、root 检查、`DRY_RUN`、结果表一律复用 `scripts/lib/common.sh`，不要另起一套输出风格。
- 必须支持 `DRY_RUN`；涉及系统写入的脚本在 `DRY_RUN` 下只打印将要执行的动作。
- 只读脚本不修改系统状态。
- 面向用户的输出使用简体中文。

```bash
#!/usr/bin/env bash
set -euo pipefail

. "$(dirname "$0")/lib/common.sh"
```

### 设备树与内核补丁要求

- 用 `git format-patch` 生成补丁文件后再放入 `patches/`。

  ```bash
  git format-patch -1 --signoff -o patches/
  ```

- 必须带 `Signed-off-by`，且与作者信息一致。
- 提交信息正文必须写清楚两件事：**为什么要这样改**（现象与根因），以及**实机验证方式**（在目标机上执行了什么、观察到什么变化）。
- 目标机的触屏问题就是这一要求的范例：boot firmware 把 TLMM `gpio174`（Himax HX83121A 接口模式选择脚）留在高电平，使 IC 处于 I²C-HID 模式，而内核通过 SPI 驱动它；在触屏默认引脚状态里把 `gpio174` 描述为低电平输出即可修复。

### 行尾必须是 LF

仓库全部文件使用 **LF** 行尾，由 `.gitattributes` 强制。开发机通常是 Windows，而目标机是 Linux/aarch64：CRLF 会让 shell 脚本在目标机上无法直接执行，也会污染 DTS 与补丁。

```bash
git config core.autocrlf false
grep -rlU $'\r' scripts docs patches   # 应无输出
```

## PR 流程

1. **说明动机**：改动解决什么问题、现象是什么、为什么需要现在做。
2. **附实测证据**：在目标机上执行的命令与完整输出。只写「已实测通过」不算证据。
3. **说明回滚方式**：若改动涉及系统状态，写清楚如何回到改动前的状态。
4. **保持单一主题**：一个 PR 只做一件事；描述使用简体中文。
5. 审查会在真实目标设备上复核，无法在实机验证的改动不会被合并。

## 安全约束

**本机型没有 EDL / 9008 救援通道。** 一旦引导或分区被破坏，没有刷机工具可以救回。

以下操作在没有设备所有者明确确认的情况下**禁止**执行，也不要让脚本默认启用：

- 修改分区表
- 覆盖 ESP
- 改写引导项
- `dd` 到内置存储

涉及上述操作的脚本必须默认 `DRY_RUN`，并要求操作者显式确认。

另外：**不要提交厂商专有驱动或固件**。这类文件不要放进仓库，也不要作为附件上传。

## 许可

- 你的贡献视为同意以 **GPL-2.0-only** 发布，见仓库根目录的 `LICENSE`。
- 内核补丁与设备树派生自 Linux 内核（GPL-2.0），请保留原有版权信息与 `Signed-off-by` 链。
- 不要提交来源不明或与 GPL-2.0-only 不兼容的代码与二进制。
