# 获取支持

本项目的支持范围**有意收得很窄**：一个机型、一个基线镜像、以实机证据为准。
这样做的原因是——同一个系列的不同变体之间，设备树、引脚配置与固件行为都不通用，
脱离实机的讨论无法得出可靠结论。提问前请先读完本文件。

---

## 1. 能问什么

| 想问什么 | 去哪 |
| ---------- | ------ |
| 脚本或内核产物在**目标机型**上失败、报错、行为异常 | 新建 **Bug report** issue |
| 确认自己手上是哪一台变体（2022 性能版 / 2022 LTE 版 / 2023 版） | 新建 **Hardware variant** issue |
| 文档中的步骤不清晰、命令跑不通、链接失效 | 新建 **Bug report** issue，并指明文档路径与小节 |
| 想贡献补丁、脚本或文档 | 先读 [`CONTRIBUTING.md`](CONTRIBUTING.md)，再开 PR |
| 发现了安全或引导风险 | **不要开公开 issue**，按 [`SECURITY.md`](SECURITY.md) 走私密渠道 |
| 行为准则相关问题 | 见 [`CODE_OF_CONDUCT.md`](CODE_OF_CONDUCT.md) 的举报渠道 |

文档索引与各文档状态见 [`docs/README.md`](docs/README.md)。

---

## 2. 提问前必须提供的四样信息

**缺任意一项都无法开始定位。** 这不是格式要求，而是因为没有实机数据的现象描述
在本项目里无法被复现，因而也无法被修复。

### ① 机型三判据

本项目只支持 **HUAWEI MateBook E Go 2022 性能版**。
市场型号字符串 **`GK-W76` 不能单独作为判据**（目标机 DMI 实际读作 `GK-W7X`，
且电商页面存在把 2023 版标成 `GK-W76` 的情况）。请给出以下三条的实际输出：

```bash
# 判据 1：设备树 compatible —— 目标机应为 huawei,gaokun3 + qcom,sc8280xp
tr '\0' '\n' < /proc/device-tree/compatible

# 判据 2：大核最高频率 —— 应为 2995200（约 2.99 GHz）
cat /sys/devices/system/cpu/cpu7/cpufreq/cpuinfo_max_freq

# 判据 3：内屏面板 —— 应为 csot,ppc357db1-4 与 himax,hx83121a
tr '\0' ' ' < /proc/device-tree/soc@0/display-subsystem@ae00000/dsi@ae94000/panel@0/compatible
```

更快的方式是直接给出前检脚本的完整输出（它会自动校验这三条）：

```bash
sudo ./scripts/00-preflight.sh
```

### ② 内核版本

```bash
uname -a
```

请一并说明内核来自哪里：本项目发布的 Release、基线镜像自带、还是自行编译。

### ③ `scripts/90-report.sh` 的完整报告

```bash
sudo ./scripts/90-report.sh > gaokun-report.txt
```

请附上该文件（或完整粘贴输出）。报告中包含的文件系统 UUID、设备序列号等信息，
如果不想公开，请在 issue 中**只保留与问题相关的部分**，其余说明已裁剪。

### ④ 复现步骤

按「做了什么 → 预期什么 → 实际什么」写清楚：

- 执行的完整命令（含参数，不要只写脚本名）；
- 预期的结果；
- 实际观察到的结果（**原始输出或截图**，"没有效果""不工作"不属于证据）；
- 该步骤是否可以重复触发，出现频率如何。

> **只写「已实测通过」不算证据。** 本项目要求可复现的命令与原始输出。

---

## 3. 明确不处理的内容

以下类别**不予受理**，请勿提交：

| 类别 | 说明 |
| ------ | ------ |
| **非目标变体** | **2022 LTE 版**（Snapdragon 8cx Gen 2 / SC8180X / `gaokun2`）与 **2023 版**（8cx Gen 3 降频版，最高约 2.69 GHz）。设备树、频率与热设计均不同，本项目的结论不适用 |
| **官方 Ubuntu ISO 环境** | 官方 ISO 在本机型上**无法正常启动**：本机在通用 UEFI 下默认走 ACPI，而该 ACPI 表不描述 MDSS / DSI 面板 / 时钟 / 互联；主线设备树也没有接入内屏。原因见 [README §3](README.md#3-基线系统)。基于官方 ISO 的失败报告不属于本项目范围 |
| **纯理论讨论** | 不打算在真机上验证的架构设想、方案权衡、"如果……会不会更好"之类的讨论。本项目以实机结论为准，没有实机支撑的提议不会被合入 |
| **要求代做硬件改装** | 焊接、更换模组、改电路、拆机取件的可行性咨询与代做请求。这超出软件适配项目的边界，也无法在维护者侧验证 |

此外，以下情况请先自行排查后再提问：

- 已按 [`SECURITY.md`](SECURITY.md) §2.2 判定为**高危操作**且未做备份的机器 —— 请先说明当前状态；
  本机型**没有 EDL / 9008 救援通道**，分区表或 ESP 被破坏后无法救回。
- 修改过设备树、内核或固件的环境 —— 请先复现到基线环境，再报告问题。

---

## 4. 期望与边界

- 本项目由社区维护者利用业余时间维护，**不承诺响应时间**；
  可参考的期望是尽力在 7 天内给出初步回应，具体见 [`SECURITY.md`](SECURITY.md) §3.3。
- **无法在目标设备上复现的改动不合入**，这条同样适用于 issue 的处理方式：
  如果问题无法在目标设备上复现，维护者只能记录现象，无法给出结论。
- 提问时请保持礼貌与耐心，交流遵循 [`CODE_OF_CONDUCT.md`](CODE_OF_CONDUCT.md)。
- 维护者与贡献者的角色划分见 [`GOVERNANCE.md`](GOVERNANCE.md)，
  维护者名单见 [`MAINTAINERS.md`](MAINTAINERS.md)。

---

## 5. 提问模板（可直接复制）

```markdown
### 机型
- [ ] 2022 性能版（唯一受支持）
- [ ] 2022 LTE 版
- [ ] 2023 版
- 三条判据输出（`tr` / `cat` 命令的原始输出）：

### 环境
- 基线镜像：
- 内核：`uname -a` 输出
- 是否修改过设备树 / 内核 / 固件：

### 现象
- 执行的命令：
- 预期结果：
- 实际结果（原始输出）：

### 诊断报告
- 附件：`gaokun-report.txt`

### 复现
- 是否可重复：
- 复现频率：
```
